<resources>
    <!-- Base application theme. -->
    <style name="Theme.SongList" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorOnPrimarySurface">@color/colorOnPrimary</item>
        <item name="colorOnSurface">@color/colorOnPrimary</item>

        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>

        <!-- Status bar -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeBackground">@color/colorPrimaryDark</item>

        <item name="backgroundColor">@android:color/background_light</item>
        <item name="colorOnBackground">@color/colorOnBackground</item>

        <!--  Dialog  -->
        <item name="buttonBarPositiveButtonStyle">@style/DialogButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/DialogButton</item>
        <item name="buttonBarNeutralButtonStyle">@style/DialogButton</item>

        <item name="searchViewStyle">@style/Theme.SongList.SearchView</item>
    </style>

    <style name="Theme.SongList.Splash">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>
</resources>